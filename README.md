# Token-Manipulation-Hunt
Detect token-manipulation behavior by correlating identity transitions, process access and resulting privileged process. Reconstructs the relationship between the process that accessed a privileged token donor, the donor process, and the new SYSTEM process, rather than rely on a specific executable name. Local (PowerShell) and Entra (Sentinel).
